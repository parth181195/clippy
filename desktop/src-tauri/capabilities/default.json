{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the Clippy panel window",
  "windows": ["panel"],
  "permissions": [
    "core:default",
    "global-shortcut:default"
  ]
}
